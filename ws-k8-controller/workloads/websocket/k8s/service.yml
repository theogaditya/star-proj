apiVersion: v1
kind: Service
metadata:
  name: websocket-service
spec:
  type: ClusterIP
  selector:
    app: websocket-server
  ports:
    - name: websocket
      port: 8765
      targetPort: 8765
    - name: metrics
      port: 8080
      targetPort: 8080
